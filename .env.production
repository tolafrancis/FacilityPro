# Public build settings for production (`npm run build`). Only PUBLIC values
# belong here: everything prefixed VITE_ is compiled into the web bundle.
# Secret keys are Supabase Edge Function secrets, never here.

# Cloudflare Turnstile site key (public). Its secret key is the Edge Function
# secret TURNSTILE_SECRET_KEY and the Supabase Auth CAPTCHA secret.
VITE_TURNSTILE_SITE_KEY=0x4AAAAAAFC3Q7Rai1GiY9zd
